---
import { SITE, CATEGORIES } from '../config/consts';
import ThemeSwitcher from './ThemeSwitcher.vue';
import WeatherWidget from './WeatherWidget.vue';
import SiteLogo from './SiteLogo.astro';
import { withBase, isActivePath } from '../lib/paths';

const navLinks = [
  { href: withBase(''), label: '首页' },
  ...Object.entries(CATEGORIES).map(([key, val]) => ({
    href: withBase(key),
    label: val.label,
  })),
  { href: withBase('about'), label: '关于' },
];

const pathname = Astro.url.pathname;
---

<header class="header-bar">
  <div class="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-4 sm:px-6">
    <a href={withBase('')} class="group flex shrink-0 items-center gap-2">
      <SiteLogo />
      <span class="font-display text-lg font-bold tracking-tight text-heading">{SITE.title}</span>
    </a>

    <nav class="hidden items-center gap-1 md:flex">
      {
        navLinks.map(({ href, label }) => {
          const active = isActivePath(pathname, href);
          return (
            <a
              href={href}
              class:list={[
                'flex items-center gap-1.5 rounded-full px-4 py-2 text-sm font-medium transition',
                active ? 'nav-link-active' : 'nav-link',
              ]}
            >
              {label}
            </a>
          );
        })
      }
    </nav>

    <div class="group flex items-center gap-2 sm:gap-3">
      <WeatherWidget client:load />
      <ThemeSwitcher client:load />
      <a
        href={SITE.github}
        target="_blank"
        rel="noopener noreferrer"
        class="btn-ghost hidden sm:inline-flex"
        aria-label="GitHub"
      >
        GitHub
      </a>
    </div>
  </div>
</header>
